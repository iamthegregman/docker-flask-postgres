version: "3"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app/:/home/app/
    depends_on:
      - db
  db:
    image: postgres:17.4-alpine
    env_file: .env
    expose:
      - 5432
    environment:
      - POSTGRES_HOST_AUTH_METHOD=scram-sha-256